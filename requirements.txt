# Core runtime
# (Phase 0-1 CLI needs no third-party packages — stdlib only.)

# Phase 2 — persistence
SQLAlchemy>=2.0

# Phase 3 — API
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0
python-multipart>=0.0.9

# Phase 7 — reporting
Jinja2>=3.1
weasyprint>=61.0

# Phase 8 — AI layer
anthropic>=0.40

# Phase 9 — auth
passlib[bcrypt]>=1.7
pyjwt>=2.8

# Phase 11 — scheduler
APScheduler>=3.10

# Dev / test
pytest>=8.0
httpx>=0.27
